crownlander_missions = {
    slot = 4                  # Which column the missions will appear in. 1 to 5.
    generic = no             # Whether missions within this series are considered generic.
    ai = yes                  # Whether the AI will claim missions in this series.
    #has_country_shield = yes  # Whether to display the country shield on the icon.    
    
    # Determines when a series appears for a country. Country scope.
    potential = {
		OR = {
			primary_culture = men_of_the_crownlands
		}
		NOT = {
			OR = {
				#tag = A10
			}
		}	
	}
    
    # The name of the mission, used for localization
    crownlander_shadow_of_kings_landing = {
		ai = yes
		icon = mission_kings_landing
		position = 1
		
		provinces_to_highlight = {
			province_id = 226
		}
		
		trigger = {
			
			OR = {
				any_owned_province {
					development = 30
				}
				226 = {
					owned_by = ROOT
				}
			}
			
		}
		
		effect = {
			add_prestige = 75
			add_legitimacy = 75
			add_country_modifier = {
				name = shadow_of_kings_landing
				duration = -1
			}
		}
	}
	
	crownlander_cultural_melting_pot = {
		ai = yes
		icon = mission_crowd
		position = 2
		
		trigger = {
			num_accepted_cultures = 3
		}
		
		effect = {
			add_prestige = 50
			add_country_modifier = {
				name = tolerant_state
				duration = -1
			}
		}
	
	}
	
	crownlander_the_royal_fleet = {
		ai = yes
		icon = mission_got_fleet
		position = 3
		
		trigger = {
			num_of_heavy_ship = 5
			num_of_light_ship = 25
			num_of_galley = 20
			num_of_transport = 10
		}
		
		effect = {
			add_country_modifier = {
				name = the_royal_fleet
				duration = -1
			}
		}
	}
	
	crownlander_seat_of_the_faith = {
		ai = yes
		icon = mission_sept_of_baelor
		position = 4

		provinces_to_highlight = {
			province_id = 27
		}
		
		trigger = {
			is_papal_controller = yes
			is_previous_papal_controller = yes
		}
		
		effect = {
			add_prestige = 75
			add_legitimacy = 75
			
			add_country_modifier = {
				name = favoured_by_the_seven
				duration = -1
			}
			
		}
	
	}
	
}



